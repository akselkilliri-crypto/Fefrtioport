[env:esp32dev]
platform = espressif32
board = esp32dev
framework = arduino

# Подключаем необходимые библиотеки
lib_deps =
    h2zero/NimBLE-Arduino @ ^2.2.1
    lemmingDev/ESP32-BLE-Gamepad @ ^0.6.2

# Эти настройки заставят собирать только .bin файл
board_build.partitions = default.csv
